# Node
node_modules/

# Java / Maven
target/

# Logs
*.log

# ── Secrets & Environment Variables ─────────────────────────────────────────
# NEVER commit these files — they contain real credentials
.env
.env.local
.env.*.local
backend/.env
scripts/.env

# .env.example IS safe to commit (placeholder values only)
# DO NOT add .env.example here

# Spring Boot local profile (contains real credentials)
application-local.yml
**/application-local.yml

# OS files
.DS_Store
Thumbs.db